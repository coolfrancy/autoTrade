i would have 1 for the specific script,, one that shows the buy and sell signals
create auto

--later-- get the auto attach to channel working to get real time data, 

create a def for getting the info from charles

ctreate a def for making it into a database using database

create a def for strategies

create one for plotting it 


i will create a class which will be able to have methods to see the chart, all classes will need parameter for its timeframe which will be editable later, the class will be able to see the char include its one strategy in it using that get class, the class will also have methods fr getting data, i can call it get info cdata class